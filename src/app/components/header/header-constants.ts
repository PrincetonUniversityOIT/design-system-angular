import {prefix as PREFIX} from '../../config';

// Main Menu Selectors
export const HEADER_SELECTOR = `.${PREFIX}-header`;

// Search Selectors
export const SEARCH_PANEL = `.${PREFIX}-header__search-bar-panel`;
export const SEARCH_SELECTOR = `.${PREFIX}-header__search-bar-toggle`;

// Styles to show menu in high resolution view
export const SEARCH_SHOWN_STYLE = `${PREFIX}-header__search-bar-panel--shown`;

// Icons
export const ICON_SELECTOR = `.${PREFIX}-icon`;
export const ICON_CLOSE = `${PREFIX}-icon-close`;
export const ICON_SEARCH = `${PREFIX}-icon-search`;
